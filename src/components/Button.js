import React, { Component } from 'react'

class Loader extends Component {
    render() {
        return (
            <button className='px-8 py-2 tracking-wider uppercase bg-primary-500 text-white rounded-full'>
                Join now!
            </button>
        )
    }
}

export default Loader
